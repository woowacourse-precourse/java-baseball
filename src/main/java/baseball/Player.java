package baseball;

public class Player {
    private int playerNumber;

    public Player(String input) {
        playerNumber = checkNumberInput(input);
    }

    private int checkNumberInput(String playerNumber) {
        // 입력 확인 -> 예외 발생
        // 정상 입력이면 숫자 세자리 반환
        return 0;
    }
}

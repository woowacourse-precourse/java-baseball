package baseball.compute;

public class cycle {
    public void restart(int sign) {
        if (sign == 1) {
            //다시 시작
        }
        if (sign == 2) {
            //완전종료
        }
    }
}
